import java.io.*;

class PowerOfTwo 
{
	public static void main(String a[])
		{
        		int power = 32;
        		double result = Math.pow(2, power);
        		System.out.println("2^" + power + " = " + result);
    		}
}
